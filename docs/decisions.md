# 決定事項ログ

仕様書（docs/game-spec.md＝依頼書v1.1）にない判断・変更・追加をした場合、ここに日付つきで残す。
依頼書 §12-5「本書と異なる実装をした箇所の一覧」はここに集約する。

## 2026-06-12 初期化・調査に基づく決定

- リポジトリ初期化。kit（chameleonjp_browser_game_kit）の CLAUDE.md・.claude/・.gitignore を継承し、kuma固有ルールを CLAUDE.md 末尾に追記
- .claude/settings.json は kit の `ask: Bash(git push *)` を外した johba 版を採用（リモート自律実行環境では確認プロンプトに応答できず停止するため。deny項目は維持）
- 【依頼書§9と異なる点・§0.3雛形優先による】ランキングは §9.2 の専用テーブル `kuma_fp_scores`＋RLS案ではなく、kit/実運用共通の **games登録＋RPC方式**（submit_score / get_best_score_ranking）を採用。RLSポリシーやテーブルの新設SQLは不要となり、納品SQLは `public.games` への登録1件になる
- 【同上】タイブレーク：依頼書は created_at 昇順だが、共通RPCの返す `rank_no`（同率順位対応）をそのまま表示する（kit ranking-spec §6）
- 【同上】「上位10件」は共通RPCの集計単位＝**プレイヤー毎ベスト**での上位10件になる（同一プレイヤーの複数記録は並ばない）
- 【同上】「あなた：◯位」は p_limit=100 で取得した一覧から display_name 一致行の rank_no を表示する近似実装。同名プレイヤーがいる場合は正確でないこと、100位圏外は「あなた：100位圏外」となることが制約
- 送信は依頼書 §9.3 どおり**手動「ランキングに登録」ボタン**（kitは自動送信も許容しゲーム仕様の選択に委ねるため、依頼書を尊重。johba=自動送信とは異なる点として報告）
- Supabase接続方式は **RESTを直接fetch**（supabase-js CDN不使用。kit雛形・johba・実験場docsのRESTフォールバック規定に一致。依頼書§2の「採用方式の報告」対象）
- 実験場URLは依頼書§4.1で【要記入】のため、kit docs/lab-integration.md 定義の `https://chameleonjp.codeberg.page/chameleonjp_lab/` を CONFIG 定数 `LAB_URL` に設定（要確認）
- GAME_SLUG=`kuma_no_fpsagashi`（リポジトリ名準拠・要確認）、CLIENT_VERSION=`v1`、localStorageキー=`kuma_no_fpsagashi_player_name`
- console.log は納品時ゼロ（§11）。ただし Supabase 失敗時の console.warn のみ johba 前例に倣い許容（デバッグ出力ではなく運用時のエラー可視化）
- 途中版ファイル（index_v2.html等）は初回リリースのため作らない。次回以降の大改修時に kit 慣行どおり作成。開発履歴は本リポジトリの git コミットで担保

## 2026-06-12 仕様の実装定義（依頼書に明記がない細部）

- 階段：乗ると「降りる／とどまる」確認ダイアログを表示（誤降下防止。ダイアログ表示はターン消費なし）
- フロア生成：32×20を4×2セクションに分割し、部屋サイズは幅3〜6×高3〜6（仕様の最小3×3〜最大8×6の範囲内）。L字通路で全域木＋冗長辺、BFSで全床連結を検証し不合格は再生成
- 斜め移動の角抜け禁止＝「移動先が斜めのとき、両直交隣接タイルがともに床である場合のみ可」。敵の移動にも同ルール
- 壁・敵のいるマスへの方向入力：向きの更新のみ（ターン消費なし・自動攻撃なし）。攻撃は攻撃ボタンのみで、空振りも1ターン消費。初期の向きは下
- 敵の視界＝「同じ部屋にいる」または「8近傍1マス」。見失ったら最後の位置の追跡はせずランダム徘徊（仕様§8.3共通AIの素直な実装）
- ハチのむれ「直進」＝視界内のプレイヤーへ貪欲に1歩近づく共通接近の実装
- ずるいキツネ：アイテムが残っている最寄りの部屋を目的地に徘徊。プレイヤーを追跡せず、隣接時のみ攻撃
- いかくカラス：1ターンおきに行動（湧いたターンを基準にパリティ固定）
- とげとげヤマアラシの反撃：プレイヤーの**隣接攻撃**のみ対象・固定1ダメージ・撃破された場合（HP0）は反撃しない。投擲では反撃しない
- ねむりヘビ：攻撃が**命中した**とき20%で眠り2ターン。眠り中の再付与は残りターンを2に上書き。眠り中は入力無効で敵ターンが約350ms間隔で自動進行
- ぬしのイノシシ：8方向の直線上に遮蔽なしでプレイヤーがいるとき2マス突進（プレイヤー隣接や障害物で停止）。突進がそのターンの行動。隣接していれば通常攻撃
- 経験値はプレイヤーが倒した敵のみ（敵同士の戦闘はない）。レベルアップは最大HP+5かつ現在HPも+5（新最大値でクリップ）。基礎防御=1+floor(レベル/3)（Lv3,6,9…で+1）。Lv30で打ち止め（経験値は加算され続けるが昇級しない）
- アイテム抽選：その階で出現可能な種類から等確率（B1〜7=月のツメ/ぬしの毛皮を除く10種、B8以深=12種。「B8以深」はB8を含む）
- 罠の配置：部屋の床のみ。階段・プレイヤー初期位置・アイテムと重複しない。プレイヤーのみ発動（敵は発動しない）。発動後は可視化され残存（ぐるぐる落とし穴も残存）
- ぐるぐる落とし穴の転移先：同フロアの空き床タイル（敵・階段タイルを除く）から無作為
- 敵の初期配置：プレイヤーの周囲2タイル以内を避けた部屋床。追加湧き（20ターン毎・上限10体）はプレイヤーのいる部屋以外の部屋床
- 装備中の武器・盾は持ち物10枠に含む。装備中アイテムは「置く」不可（メッセージ表示）。装備（付け替え）は1ターン、同種は自動で入れ替え
- 投擲は投擲分類（とがった松ぼっくり・しびれ茸）の「使う」で8方向選択→直線飛翔。壁に当たった場合は消滅（落下しない）。投擲も1ターン
- ひかる木の実をHP満タンで使用：最大HP+2し、現在HPも新しい最大値まで回復
- 満腹度警告：30以下へ低下した時点と0になった時点で各1回メッセージ（毎ターン連発しない）。食料で31以上/1以上へ回復したら警告状態はリセット
- B16以深の補正 `ceil(基礎×(1+0.1×(階−15)))` はHP/攻撃/防御/経験値に適用（防御0は0のまま）
- 死因表記：敵=「◯◯にやられた」／とげの床=「とげの床で力尽きた」／飢え=「おなかがすいて倒れた」
- ミニマップの階段は視認（踏破）済みの場合のみ表示
- 独り言は頭上吹き出し方式（Canvas描画・4秒）。20秒は実時間で、メニュー・結果・ホーム中は累積を停止（リセットしない）。リシャッフル境界で直前と同じ行が先頭に来たら入れ替えて連続を回避
- メッセージログは直近3行を表示
- 攻撃の向き＝直前に入力した方向（移動の成否を問わず更新）

## 2026-06-14 リビジョンv2（依頼者フィードバック）

依頼者の明示指示による、依頼書v1.1からの変更。詳細計画は docs/REVISION_PLAN.md。

- 【§8.3からの変更】深層に新規大型獣10種を追加（🐃水牛12 / 🦍ゴリラ15 / 🐅トラ18 / 🦏サイ22 / 🦛カバ25 / 🐘ゾウ28 / 🐊ワニ30 / 🦣マンモス33 / 🫎ヘラジカ36 / 🐉ドラゴン39）。各 intro階から次の獣の手前まで出現し、intro階基準・rate0.06の緩やかな床上昇。「理論上撃破可能」をハーネス beatable グループで検証（intro階は回復なしタイマンで勝利可能）
- 【§5.2の置換】ねむりヘビ・ぬしのイノシシの maxF を Infinity→15 に制限。B16以深は新規獣が担当（§5.2の無限スケール記述は新規獣方式で実現）
- 36階の絵文字は依頼の「🫈」が非標準のため 🫎（ヘラジカ）と解釈（要確認）
- 【§8.1の変更】HP自然回復を 2ターンに1回 → **5ターン（5歩）に1回**（REGEN_EVERY_TURNS 2→5）
- ぐるぐる落とし穴は「通路に隣接する部屋床（出入り口）」には置かない（回避不能な強制ワープを防ぐ）。とげ/はらぺこは従来どおり
- 罠ログに効果量を明記（とげ「5ダメージ」/ はらぺこ「満腹度が20減った」）。撃破時に「経験値を N かくとく」を表示
- メッセージログ表示を 3行→5行（熊の攻撃が敵の反撃で押し出されにくいよう）
- 「移動毎-1ダメージ」報告はコード欠陥ではないと再現確認（敵全滅後はHP回復）。原因は高防御の熊に弱い敵1体が追尾して最低1ダメージを与え続けるのが、旧3行ログで見えにくかったこと。HPバー・向き矢印・ログ増行・戦闘エフェクトで可視化して解消

## 2026-06-14 リビジョンv3 計画確定（依頼者承認）

設計会議（3役割: ゲームデザイン/世界観/システム）の意見を docs/IMPLEMENTATION_PLAN_v3.md に統合。依頼者が以下を承認:
- ダメージ式は**指数式にしない**。減算式維持＋乱数幅を装備依存に＋深層敵に貫通/割合特技でB25難化。
- 実装は **F0(無回帰リファクタ)→F7(状態異常統合)** の段階方式。なりきりパン228/地形/経済/店/識別は後段で別途確認。
- **FPは非アイテムの最終ゴール**として温存。FPモチーフは最終装備セット🐾＋気配演出に限定。
- 装備は3段→多段（武器/盾とも約14段＋効果違い系統）へ拡張（§8.4の仕様追加）。
- 新カテゴリ（野草/伝え葉/ふしぎ枝/飾り/ころがし石/矢）を段階導入。総量は把握可能上限の約60〜90種。

## 2026-06-14 v3 F1完成・F2（効果装備・投擲・FP装備）

- F1完成: 効果違い装備6種（🌸ねむり花/🪓こぐま/🍯みつぬり/🪶はやて ＋ 🌵とげ/🌩️いかずち）。onHitに lifesteal/extraStrike、onAttacked counter、onAttackCost satiety。フック機構 `_gearEffects` で「装備が増えてもCOREは増えない」。
- F2: 投擲に貫通(pierce)対応。🥌ころ石(dmg5)・🏹月羽の矢(dmg14/貫通/B12+) を追加（cat:'throw'再利用）。
- FP最終装備セット🐾（深層B30+限定・到達の証）: FPのツメ(HP低下で攻撃up＝背水)・FPの毛皮(致死を1フロア1回しのぐ「かばう」)。FP本体は非アイテムのゴールとして温存（承認方針）。
- `_lethalGuard` を全死亡判定（敵攻撃/飢え/とげ罠/ヤマアラシ反撃）に挿入。run.fpGuardUsed をフロア毎リセット。
- 全759合格（gearfx拡充・投擲のflakyテストを内側固定位置で安定化）・難易度カーブ維持。

## 2026-06-14 v3-1 装備の段階拡張（実装中）

- 武器(ツメ)・盾(毛皮)を各7段→**14段**に拡張。豊富な原作風名（木の枝/きば/岩/こおり/月/あらし/かみなり/はがね/やまなり/ぬし/りゅう/まおう/でんせつ/ほし）。
- 出現階バンドを再設計し「各階で入手可能な最良装備のパワー」を v2.2 と概ね同等に保つ（過剰強化＝B25緩化を避ける）。深い階ほど強い段階だけ落ちる方針は維持。
- 本増分は名前/段階のみ。ダメージ式の乱数幅装備依存化・特殊効果装備は後続増分。

## 2026-06-14 リビジョンv2.2（装備の多段階化・B25以降の難化）

依頼「B25が緩い／落ちてる装備のバリエーションが少なく単調／深い階ほど強い装備に／風来のシレン参考」。
参考URL（sfc-frontmission.kouryaku.red）はHTTP 403で取得不可だったため、シレン系の作法（装備の段階制・出現階バンド）を知見で実装。

- 武器を3→7段階、盾を3→7段階に拡張。各装備に出現階バンド `[minF,maxF]` を付与し、
  「深い階ほど強い装備だけが落ちる」を実現（弱い段階は浅い階で打ち止め）。
  - 武器: 木の枝のツメ+2(B1-6) / 岩+5(3-12) / 月+9(8-18) / はがね+13(13-25) / りゅう+18(19-32) / まおう+24(26-40) / でんせつ+31(33-)
  - 盾: ふかふか+2(1-6) / こわい+5(3-12) / ぬし+8(8-18) / はがね+12(13-25) / りゅう+16(19-32) / まおう+21(26-40) / でんせつ+27(33-)
  - 各階で2段階が重なって出る＝バリエーション。実測: B2は+2、B12は+5/+9、B25は+13/+18、B39は+24/+31。
  - 「同じ絵文字でも中身が違うのはアリ」との許可に基づき新絵文字（⚔️🗡️🔱🪓🛡️🐲👹🧱）を割当（既存と重複なし）。
- 深層回復 `大きな木の実`(HP+50, B12-) を追加し、深い戦いの資源を補強。
- アイテム選択を出現階バンド `minF<=F<=maxF` で絞る（食料/回復/投擲は maxF なし＝常時）。
- 【B25緩い への対応】B22以深の獣を大幅強化（例: のしかかりカバ B25 を 攻18/HP70 → 攻36/HP135、スケール0.07-0.08）。
  装備が強くなったぶん敵も強くし、熟練プレイヤー(Lv20+・最良装備)でもB25のタイマンで約40%HPを失う＝群れは戦術必須に。
  検証: ハーネス beatable（最良装備＋深層は回復1個ぶんの許容）で各獣のタイマン撃破可能性を担保しつつ、与/被ダメ分析でB25 40%・B30 47%・B39 79%HPと難度が右肩上がりであることを確認。
- 注: balance-sim のボットは人間ほど上手くXP稼ぎ/位置取りできず、強化後はB20止まり（B25 0%）。実際の難度は熟練プレイヤー基準（分析）で設計。実機での体感は要プレイテスト。
- ハーネス：gearグループ追加（段階性・バンド・深層で弱装備が出ないこと）。全734合格。

## 2026-06-14 リビジョンv2.1（難易度カーブ再設計・戦闘の単調さ解消）

依頼「5層以降は難易度が高く、25層以降は最適解の行動をとれば勝てる」「戦闘が単調」への対応。
`tools/balance-sim.mjs`（開発用・非公開）で naive（脳死で殴る）と smart（回復・しびれ・通路で1対1・撤退・XP稼ぎ・装備更新）の2ボットを各500回自動プレイし、難易度カーブを数値で測りながら調整。

- 診断：旧バランスは過酷すぎ（平均Lv3で誰もB16を越えられない＝経験値が steep すぎて低レベル詰み）。これが「単調」の主因でもあった（強くなれず・生き残れず、戦術を使う前に死ぬ）。
- 【§8.1の変更】経験値式を `10×1.4^(n−1)` → `6×1.20^(n−1)`（XP_BASE 10→6, XP_MULT 1.4→1.20）。低レベル詰みを解消し、深層で戦術が活きる地力を持てるように。
- 【§8.1の変更】初期HP 15→20、レベルアップHP +5→+6（序盤の生存性）。
- 敵の数・湧き・上限を階スケール化（Core.enemyInitCount / spawnInterval / enemyCap）。B1-2は2体・B1-4は楽、B5以降に圧、深層は群れ（最大13体・湧き12ターン）。旧 ENEMY_INIT_*/SPAWN_EVERY_TURNS/ENEMY_CAP は廃止。
- アイテム数を B10+ で 3-5 に増量（深層の資源＝最適行動を支える）。
- 結果カーブ（balance-sim 500回）：B1-4は両者~87%生存／naiveはB10 56%→B15 37%→B20 0%（中盤で失速・B15-20が壁）／smartはB15 61%・B20 22%・B25 8%（戦術でnaiveの約2倍潜れる）。深層1対1の撃破可能性はハーネス beatable（到達相応レベル Lv≒6+0.45F で検証）で担保。
- 戦闘の単調さ：naiveとsmartの到達差（約2倍）が「戦術が必須」を示す＝回復/しびれ/松ぼっくり/通路で各個撃破/撤退/装備更新/XP稼ぎを使い分けないとB15以降は越えられない設計に。
- 検証：core-harness 714全合格（bot最深B22-23）・DOM起動/プレイ/終了/自動送信 例外なし。

## 2026-06-14 リビジョンv2 バッチC/D（メニュー・ランキング）

- 【§9.3-1からの変更】ランキングは結果画面での任意手動送信ではなく、**ゲーム完了と同時に自動送信**（依頼者指示）。二重送信防止・送信中/成功/失敗表示・失敗時のみ再試行 は維持。名前は開始時入力済み（既定「ななしのくま」）
- 結果画面の死亡コメントの「FP」表記を「ファーストパートナー」に変更（タイトル「熊のFP探し」とシェア文は不変）
- メニュー刷新（C1-C8）：持ち物見出しに所持数 N/10（装備中も枠に含む＝§8.4据え置き）／アイテム名タップで効果説明（食料は満腹度+値）／装備品のボタンは「装備」／装備中アイテムは持ち物一覧から除外し装備タブに表示／装備タブは武器・盾の各部位に現在装備＋「装備」「外す」ボタン／「装備」で持ち物内の装備可能品を選択肢表示・無ければ「装備できる持ち物がない」／装備・外す・付け替えはメニューを閉じず実行（C8。1ターン消費＝§8.4据え置き、敵が1行動）
- 装備中アイテムは「置く」できない（従来どおり）。装備の付け替え・外すは各1ターン消費
- 操作ボタン拡大：dpad 48→56px・攻撃 64→76px・足踏み/メニュー 44→52px

## 2026-06-13 監査時の決定・補足

- しびれ茸の麻痺は敵フェーズ**前**に付与する（投擲ターンに反撃を許さない＝麻痺アイテム本来の即時行動阻止）。当初ハーネスはstun=5の即値を見ていたが、隣接敵への反撃が発生する実装になっていたため、麻痺の本質（反撃なし・5ターン行動不能）を検証する内容へ改めた。松ぼっくり（ダメージ投擲）は敵フェーズで通常どおり反撃が起こる
- 独り言タイマーの tick は `dt < 1000ms` のフレームのみ累積する（バックグラウンドタブ復帰時の大きな時間跳躍を無視。20秒間隔が一度に飛ばないようにする保険）
- メニュータブ（持ち物/装備/状態の切替）も操作ボタンとして min-height:44px を確保（依頼書§3「すべての操作ボタンは44px以上」）
- 実験場 `games` 登録の表示用スコア設定：score_order=desc（深いほど上位）/ top_ranking_type=best / score_unit=「階」/ score_scale=1 / score_decimals=0。
  ゲーム内ランキングは「B◯F」形式だが、実験場トップ/詳細ページの汎用レンダラはプレフィックス「B」を持たないため「◯階」表示になる見込み（表示の細部は実験場側仕様。要確認）
- 本環境からは Codeberg Pages（chameleonjp.codeberg.page）と Supabase（*.supabase.co）へネットワーク到達不可のため、実験場ページの実反映・RPCの実通信は未確認。コード作法は kit雛形・実運用johbaと一致させ、納品時に実機/実通信確認を依頼する

